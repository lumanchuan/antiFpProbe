# 复现包验证记录

[English](VALIDATION_EN.md) | 简体中文

整理日期：2026-09-15。验证平台：Tofino1 主机，SDE 9.7.0。原工程保持不动；初始化、依赖安装、编译和网页启动测试均在额外的临时副本内执行，副本目录名与交付目录不同。

## 已完成

| 检查 | 结果 |
|---|---|
| Nmap 控制面单元测试 | 17 项通过 |
| p0f 指纹规范化单元测试 | 6 项通过 |
| 网页、进程管理、采集隔离、目录迁移回归测试 | 60 项通过 |
| Nmap P4 独立编译 | 通过，产生 BFRT、context、tofino.bin 和独立配置 |
| p0f P4 独立编译 | 通过，与 Nmap 产物分离 |
| scan_monitor P4 独立编译 | 通过，已去除对旧 direct_test 目录的 include 依赖 |
| doctor 自检 | SDK/Python ABI、三个构建配置、活动样例均通过 |
| Python / shell 语法 | SDE 关键脚本用 Python 3.5；网页/工具用 Python 3.9；shell 用 bash -n 检查通过 |
| 网页页面与静态资源 | 首页、Nmap、p0f 及其引用的本地静态资源请求成功 |
| API | 登录、Nmap/p0f 指纹列表、运行状态接口通过；没有输出令牌 |
| 网页真实启停 | 随机本机端口启动，HTTP 访问成功，管理脚本准确停止自己创建的进程 |
| p0f 采集器打包 | 在内存中检查生成代码可编译，未在 NIC 上执行采集 |
| 保守源码归档 | 成功导出，检查不含 runtime、build、令牌及未确认的 SDK 辅助头文件 |
| 原文件保全 | 初始选取的 107 个源文件 SHA-256 与原工程对应文件一致 |
| 交付目录清洁度 | 无运行数据库、令牌、SSH 密钥、旧构建二进制；没有临时目录名/原工程绝对路径依赖 |

合计 **83 项单元/回归测试通过**。Nmap 测试中故意注入 RPC 失败及错误 JSON 来验证回滚、保留旧规则，测试输出中的相应异常是预期测试场景，不是实际硬件下发失败。

网页目录带有 47 个 Nmap 指纹记录和 10 个 p0f 指纹记录；不兼容记录仍受兼容性检查限制。此数量是目录/API 检查结果，不是声称所有记录都通过了硬件识别实验。

## 编译警告

Nmap 保留原代码中的表容量收缩警告（有限匹配位宽导致实际容量小于声明值），以及 ICMP 键切片被编译器替换为字段名称的提示。p0f 保留 `plain_syn` 键切片名称替换提示。三个编译命令均成功返回，监测转发编译日志无上述警告。未为消除警告而重写算法或改变原实验方案。

## 本次未执行

- 没有加载/切换新 P4 管线到 ASIC。
- 没有停止原 5080 网页、原数据面/控制面或 NIC 上的其他程序。
- 没有改动网卡驱动、IP、路由、ARP、防火墙、SSH 配置。
- 没有逐条重新执行 Nmap/p0f 硬件识别与 TCP 业务传输实验。
- 没有完成第三方权利的法律审核；该次验证运行本身不执行代码上传。

因此，本记录证明的是**源码包独立构建、目录迁移、控制逻辑与网页部署流程可用**，不是新环境上所有指纹都能精确伪装的保证。正式演示前请按 Nmap/p0f 文档完成端到端实验，并记录自己的软件版本、指纹和观察结果。

## 重跑命令

先配置、编译并安装独立网页环境，再在项目根目录执行：

```bash
/usr/bin/python3 -m unittest discover -s tests
/usr/bin/python3 -m unittest discover -s test_p0f -p test_profile_unit.py
.venv/bin/python -m unittest discover -s HTML/tests -p 'test_*.py'
/usr/bin/python3 scripts/doctor.py
```

测试会在本目录的 `HTML/runtime` 内创建隔离临时数据。要完全避免与运行中的演示共享运行目录，请另复制一份项目并初始化后测试。测试中的进程停止验证只针对它自己创建的临时模拟进程。
